# 最终整合复核

## 唯一基线与分支

本次最终复核基于已合并 PR #1 的 `main`：
`7b511f9e7aa63e84235d52c4a66b6a13b9a4587f`。
检查过程中发现并行会话完成了该合并，因此放弃旧基线上尚未提交的替代改动，
保留主线的 24 课、72 个手动教学快照及现有历史模块。此前八课 ZIP 不属于此主线。

`main` 是唯一长期维护分支。此次只清理两个已经审查并合并的分支：

| 分支 | 允许删除的精确末端 |
| --- | --- |
| feat/curriculum-audit-and-session-resume | 7fab30b657b10073dc228d6e4980d2cc3dd2044e |
| feat/interactive-harness-lab | a6319bc134fe6ae7adf6a6ef02117623f965c673 |

`cleanup-branches.mjs` 仅在带 `[consolidate]` 的主线提交且全部质量检查通过后执行。
它检查仓库、主线版本、上述精确末端及祖先关系，以带租约条件的原子推送删除。
若其他会话改动任何目标分支则拒绝清理，不覆盖新提交；main 从不作为删除目标。
以实际工作流和远端分支列表为完成证据，脚本存在不代表清理已发生。
未来贡献使用短期 PR 分支，合并验证后删除，不维护多套独立实现。

## 对完整提示词的验收

| 要求 | 已保留并检查的实现 |
| --- | --- |
| 完整的入门工程通路 | 24 课覆盖协议、循环、工具、上下文、RAG、记忆、Skill、协作、MCP、权限、预算、评估、部署 |
| 可视化与可交互 | 11 类本地实验；每课 3 个手动教学快照、预测题与迁移问题，共 72 个快照 |
| 概念与代码能对应 | 课程原文、代码注释、实现文件、快照观测、自测与原始来源并列 |
| 不依赖 API Key | 全课程和本地实验可免密钥运行；规则模型、真实运算、教学快照分别标明 |
| BYOK 与模型名 | 本机体验页显式填写；四套协议请求预设；没有自动模型探测 |
| 少量 Token | 每次一个手动请求，输出默认128、最高256；输入600字符；默认不带历史，不自动重试或追加工具结果请求 |
| 历史记录 | 本地保存、搜索、继续、导入导出、删除；恢复不带密钥与自动授权；20会话×40消息，导入限2MiB |
| 后续开发 | AGENTS、开发规范、架构、页面/API/测试说明、来源核验、PR及Issue模板齐备 |

## 最后修补的三处行为

1. 点击已经选中的体验模式不再清空当前未保存对话；真正切换模式仍开始新会话。
2. 浏览器拒绝删除持久存储时，不再用“已清除”的成功提示覆盖实际错误。
3. 聊天恢复判定拒绝空元素、非文本消息等无效记录，不抛出异常。

新增 `tests/final-audit.test.mjs` 与 `tests/final_browser.py`。
原有测试保持不变，没有为通过检查而降低阈值。

## 验证记录

基线合并提交的 Quality checks 运行 `34998913035` 已核验：Ubuntu Node22、
Ubuntu Node24、Windows Node22、Chromium浏览器四项作业均成功；浏览器报告
覆盖41条路由、桌面/手机、24课快照及历史恢复/导入导出，未发送真实供应商请求。
下载其实际截图检查了首页、课程和手机布局，没有用旧ZIP的截图冒充证据。

本次补丁在本地 Node22.16.0 下 `npm run verify` 通过：106项测试，无失败或跳过，
语法/内容检查和静态构建通过。完整浏览器和新增回归由同一 Quality checks 运行，
结果位于 `browser-results` 中的 `browser-report.json` 与 `final-report.json`。
每次提交的实际检查结果以 GitHub Actions 对应提交为准。

```sh
npm run verify
python tests/browser_smoke.py
python tests/final_browser.py
```

未使用真实供应商密钥或付费模型调用，没有自动发布网站。静态托管只提供课程
和免密钥实验，真实API需要本机中继。输出上限控制请求规模，不是总费用承诺。
MCP、多Agent和生产沙箱仍按文档标明为教学范围，未声称部署完整外部基础设施。

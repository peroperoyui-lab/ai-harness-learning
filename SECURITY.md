# 安全边界与报告

本项目用于个人本机学习，不是可直接暴露公网的多用户模型网关，也不执行任意模型生成代码。

## 默认边界

- Node 只监听 `127.0.0.1`。Host 必须是当前端口的 `localhost` 或 `127.0.0.1`；模型 POST 请求还要求同源 Origin、固定自定义请求头和 JSON 内容类型。
- 默认上游仅允许 `api.openai.com`、`api.anthropic.com`、`api.deepseek.com`。自定义主机须通过启动参数明确加入，无通配符、无自动发现。
- 上游仅使用 HTTPS 默认端口，不允许 URL 内嵌凭证、查询参数或片段，不跟随重定向。解析所有 IPv4 地址并拒绝私网和保留地址，再把本次验证过的地址固定到 HTTPS 连接，TLS 仍核验原始主机名。
- 只服务 `web/` 内已知类型静态文件，检查真实路径，不能读取 `.env`、服务器源码或仓库任意文件。
- 所有真实请求受服务端大小、输出预算、轮数、并发、频率和时间边界约束；详细数值见 [API 文档](docs/API.md)。真实演示没有自动 Agent 循环或自动重试。
- 只有一个固定、无外部副作用的计算器工具可执行，参数、操作名称和数值范围均检查。没有终端、文件写入、任意表达式执行或 MCP 服务器启动功能。

这些规则不能抵御已经控制本机进程、浏览器扩展或源文件的攻击者。Origin 校验用于减少其他网页发起请求的风险，不等同于完整身份认证。HTTPS 服务商、DNS 基础设施和本机系统仍属于信任边界的一部分。

## 凭证和用户内容

API Key 只存在于当前页面表单、当前请求和中继进程短暂内存中。项目不将它写入浏览器持久化、服务器日志、文件、导出或仓库。切换服务、退出真实模式、离开体验页、刷新或手动清除都会清理页面中的密钥。

请求预览完全不含认证字段。模型结果和错误进入页面前进行精确密钥匹配脱敏；持久化只允许固定字段。该脱敏不是通用秘密检测器，无法识别用户自行输入的其他密码或被变形后的秘密。不要在提示词中粘贴有效密钥或敏感个人／科研资料。

进度与主动保存的问答位于 `localStorage`，是同源明文数据，最多最近 20 个会话、每会话 40 条消息。导出文件也未加密。页面提供删除与清除操作；服务端不保存会话。供应商仍可能按自身政策记录请求内容，Responses 的 `store:false` 不代表所有服务均零保留。

## 有意保留的限制

本机中继不支持私网模型地址、HTTP、非默认 HTTPS 端口、IPv6-only 上游、任意请求头、系统代理自动发现、远程多用户访问或长期凭证保存。部分代理的 Fake-IP DNS 使用保留网段，会被阻止；应修正解析或网络设置，不要直接删除地址检查。

SSE、提示注入、多 Agent、MCP、沙箱等实验或章节具有明确教学边界。关键词提示不是完整提示注入检测；模拟审批不代表真实写入；本地状态机不代表已经部署生产级容错系统。

## 报告安全问题

有效密钥泄露时，先在供应商处撤销并轮换；删掉公开文本不能使已经泄露的凭证重新安全。

优先使用 GitHub 的私密漏洞报告渠道（仓库启用时可用）。渠道不可用时，先提交不含利用细节、真实密钥和敏感数据的最小 Issue，请维护者提供私密沟通方式。不要在公开 Issue、PR、截图或运行日志中粘贴秘密。

报告应包含受影响版本／提交、复现条件、观察到的边界失效与建议修复范围。普通课程内容错误请使用内容纠错模板。

## 历史导入与恢复

导入是不可信文本入口，采用版本/大小检查和字段白名单，不恢复连接配置或密钥，不执行内容。ID 冲突复制而非覆盖。新 task 字段仅接受 explain/json/tool；只有最近完整 explain 问答对能在用户勾选后进入一次新的解释请求。无标签旧记录可查看但不自动发送。恢复页面也不隐式同意费用或持久化更新。共享计算机上应使用独立浏览器配置，离开前清除本站数据；浏览器存储不是加密保险箱。

# 贡献指南

欢迎修正文句、补充测试、改进交互，或添加能够真正运行的小实验。先阅读 [页面目录](docs/PAGES.md)、[架构说明](docs/ARCHITECTURE.md) 和 [开发规范](docs/DEVELOPMENT.md)。

## 开始一个 PR

1. 从当前 `main` 创建 `feat/主题`、`fix/主题`、`docs/主题` 或 `test/主题` 分支。一个 PR 聚焦一个可解释的改动，不顺手重构无关模块。
2. 先复现问题或写明新功能的验收条件。修复程序问题时，增加能在旧实现上失败的回归测试。
3. 完成代码、对应课程说明和必要目录更新，运行 `npm run verify`；修改 UI 时再执行浏览器测试，并附桌面／移动端截图。
4. 按 PR 模板说明改了什么、怎样验证、有哪些限制。维护者确认后再合并。提交描述可使用中文。

建议提交前缀：`feat:`、`fix:`、`docs:`、`test:`、`refactor:`、`build:`、`ci:`、`chore:`。不要把 API Key、真实用户会话、个人研究资料或日志原样提交到公开仓库。

## 新增或修正课程

课程位于 `web/content/`，每节使用稳定的 `id`。现有 Hash URL 可能被用户收藏，请勿随意重命名。若必须更改，应同时实现旧链接重定向并更新目录与测试。

每节课必须包含：学习目标、三个实质性讲解段落、带注释代码、代码性质标签、关联实现路径、常见误区、可操作任务、自测题与答案解释、原始来源键。新增整节课程时，需要同时修改课程完整性测试中的显式数量和编号策略；不要为通过检查而删除断言。

说明语言应专业、直白，解释“它是什么、如何工作、什么情况下失效”。术语第一次出现时说明含义。避免只有口号、类比或大段无注释代码。源码片段、伪代码与概念图式必须区分，模拟结果不得写成真实模型结果。

修正文献或接口事实时，优先引用官方规范、原始论文或维护者文档。将链接及其用途加入 `web/content/course.js` 的 `sources`。标清协议版本和服务差异；不要把现价、当前模型名或短期配额写死成永久事实。

## 新增实验

实验通过 `mountLab(root, id, context)` 挂载，返回清理函数。清理函数负责取消计时器、网络等待和其他副作用。纯算法放在 `web/lib/`，DOM 逻辑放在 UI 模块，课程数据不要包含执行逻辑。

必须保留无需密钥的入口，明确显示实验的真实性边界：固定脚本、实际算法、示意数据或真实接口。一个按钮应当产生可观察的变化，不接受只有外观没有行为的空入口。失败、空输入、重置、取消与预算边界也需要可用。

真实 API 的任何自动调用、重试、输出上限提高、额外历史发送或新工具执行都属于行为变化，必须在 PR 中明确指出并经审查。不要用新的付费依赖替换可以在本地完成的教学实验。

## 修改协议或安全逻辑

同一份序列化器同时用于请求预览与服务器发送。新增协议必须同时更新请求、响应归一、错误说明、工具 Schema、用量字段、文档和合同测试。不能静默切换协议、追加重试或编造 usage。

修改网络中继需要补充拒绝测试，包括来源、主机、协议、端口、私网 DNS、重定向、请求大小、预算和取消。工具允许列表、参数验证与人工授权不能依赖模型文字。不要加入任意代码执行或公网监听作为“方便使用”的快捷修复。

## 界面与可访问性

保持系统字体、项目原创 SVG、克制动画与清楚的文字层级。不要引入遥测、外部字体、脚本 CDN 或未说明许可的素材。小屏不应横向溢出；按钮具有文字或可访问名称；状态不能只靠颜色表达；尊重减少动画偏好。动态用户或模型内容按文本渲染，不能直接作为 HTML 执行。

## 验收清单

- `npm run verify` 通过，新增行为有相应断言。
- 浏览器关键流程通过，路由、移动端、空态和错误态没有回归。
- 没有未经请求的真实 API 调用、自动重试或凭证持久化。
- 课程、实验、源码链接与 `docs/PAGES.md` 一致。
- PR 说明区分已验证、模拟验证和未验证部分。
- 外部依赖、素材与引用具有明确许可或来源。

安全问题不要在公开 Issue 中粘贴有效密钥或可利用的敏感配置。处理方式见 [SECURITY.md](SECURITY.md)。提交贡献即表示你有权贡献这些内容，并同意其按本仓库 MIT 许可发布。

export default [
  {
    id: 'context', title: '给上下文排一个优先级', subtitle: '容量、保留策略、压缩与完整消息组', minutes: 16, lab: 'context', goal: '在有限窗口中保住当前任务，并解释哪些信息被移除。',
    sections: [
      ['窗口里装的是本轮需要的信息', '应用规则、用户问题、历史、工具说明和检索片段会共同占用上下文。容量不是唯一问题：无关内容也会增加干扰。先根据任务挑选必要材料，再考虑截断或摘要，比把所有内容直接拼起来更容易审计。上下文策略应该记录保留和舍弃的理由。'],
      ['给输出预留空间', '简化预算可写成“输入预算 = 窗口容量 − 输出预留”。不同模型还可能有独立输出上限和推理 Token 约束。本实验把系统规则和当前问题设为必需项，其他内容按优先级装入；若必需项都放不下，就明确报告溢出，而不是悄悄丢掉用户问题。'],
      ['裁剪要保持语义与协议完整', '历史问答最好按完整组处理；工具请求与结果也应一起保留或一起移除。摘要节省空间但可能丢失限定条件，需要保存来源和关键决定。本实验用固定教学单位展示装箱策略，没有调用真实分词器，也不实施生产级工具依赖解析。'],
    ],
    code: [['const available = capacity - reserve;', '先减去输出预留。'], ['const required = items.filter(x => x.required);', '识别当前任务与不可丢失的规则。'], ['if (requiredCost > available) return overflow;', '必需内容装不下时停止构建。'], ['for (const item of byPriority) {', '这是贪心优先级策略，不是全局最优算法。'], ['  if (fits(item)) selected.push(item);', '仅加入仍有空间的完整内容单元。'], ['}', '被舍弃的项也需要可见。']],
    source: 'web/lib/algorithms.js', codeLabel: '装箱逻辑伪代码；实际实现见 packContext',
    pitfall: '摘要可能丢信息；“更长的窗口”也不能自动修复错误来源和冲突指令。', challenge: '将容量降到必需项与预留之和以下，检查系统是否停止而不是默默裁掉当前问题。',
    quiz: ['当前问题和系统规则都放不下时，应该怎样？', ['随机丢掉一条', '显式报告超限并调整预算', '让模型自己猜'], 1, '预算失败应成为清楚的系统状态。'], sources: ['context'],
  },
  {
    id: 'rag', title: '让回答有可追溯的材料', subtitle: '切块、检索、排序与 RAG', minutes: 18, lab: 'retrieval', goal: '观察检索分数如何变化，并区分相关性与事实正确性。',
    sections: [
      ['检索增强生成的工作链', '把文档按合适粒度切块，保留标题、出处、更新时间等元数据，再为查询找出候选片段。排序后的材料进入模型上下文，模型据此回答并引用来源。检索器负责找材料，生成器负责组织答案，引用负责把陈述连回证据。每一段都可能出错。'],
      ['先看一个能解释的检索器', '本站在几个内置短文中运行 TF-IDF 与余弦相似度：中文使用相邻双字片段，英文使用词项；越少见的匹配词得到越高权重。页面同时显示匹配词和分数。它是词面检索，换成语义相近但没有重合词的问法，可能找不到相关文档。'],
      ['向量检索也需要检验', '嵌入模型可以把文本映射到向量空间，用向量相似度召回语义候选。实际系统还可能组合关键词检索、重排和元数据过滤。相似度分数不是“内容为真”的概率。评估时分别看能否找到正确材料，以及答案是否忠实于材料，避免只看最终措辞流畅。'],
    ],
    code: [['const candidates = retrieve(query, documents, 3);', '调用本站真实词面检索器，返回最多三个非零结果。'], ['const evidence = candidates.map(d => ({', '保留证据与来源，不只保留纯文本。'], ['  id: d.id, text: d.text, score: d.score', '分数用于排序，不是置信概率。'], ['}));', '得到可审计的证据集合。'], ['// 将 evidence 放入上下文，再生成带出处的回答。', '本站此实验停留在本地检索阶段，不消耗 API Token。']],
    source: 'web/lib/algorithms.js', codeLabel: '调用真实检索器的片段；最后一步仅说明 RAG 延伸',
    pitfall: '文档里写了某句话，不代表这句话可靠；文档中的指令也不应自动获得执行权限。', challenge: '输入“工具 权限”，再输入一个无关词；改变 top-k，观察零分结果为什么不会被硬塞进上下文。',
    quiz: ['本实验的 TF-IDF 分数表示什么？', ['文档内容为真的概率', '词面向量的相似程度', '模型完成训练的程度'], 1, '本地算法只衡量特定表示下的相关性。'], sources: ['retrieval', 'context'],
  },
  {
    id: 'memory', title: '让任务在刷新后还能继续', subtitle: '短期上下文、持久记忆、检查点与历史', minutes: 16, lab: 'memory', goal: '保存并恢复一个检查点，理解存储与上下文的区别。',
    sections: [
      ['至少区分三种东西', '短期上下文是当前请求读到的信息；持久记忆是跨会话保存、按需取回的事实或偏好；检查点是某次任务的结构化进度。聊天历史是重要记录，但不能自动替代这三者。设计时先问“保存什么、谁能读、何时失效、如何删除”。'],
      ['恢复要考虑版本与副作用', '检查点通常含任务 ID、阶段、已完成步骤、结果引用和数据格式版本。应用升级后需要校验或迁移旧数据。若上一步已经写入外部系统，恢复不能简单地把它再做一遍。检查点支持继续推进，不保证对外部系统有 exactly-once 语义。'],
      ['本站如何保存学习记录', '学习进度和最多 20 个会话保存在当前浏览器的 localStorage，每个会话最多 40 条记录。真实 API 的密钥与配置不写入其中。历史可以导出和删除，浏览器隐私模式或配额限制可能使存储失败，此时页面提示并退回内存。记录不是加密保险箱，请不要输入敏感研究资料。'],
    ],
    code: [['const checkpoint = { version: 1, step: 2, result: 161 };', '用显式字段而不是一大段不可解析文字。'], ['const text = JSON.stringify(checkpoint);', '序列化为存储文本。'], ['const restored = JSON.parse(text);', '恢复对象后仍需检查版本与字段。'], ['if (restored.version !== 1) throw new Error("需要迁移");', '不要假设旧格式永远兼容。'], ['// 下一次模型调用只读取被上下文构建器选中的信息。', '持久化和发送到模型仍然分开。']],
    source: 'web/lib/storage.js', codeLabel: '检查点概念片段；项目持久化实现见 storage.js',
    pitfall: 'localStorage 是同源明文存储，不适合保存 API Key，也没有跨设备同步。', challenge: '在检查点实验推进两步并保存，重置临时状态后恢复；再去历史页确认保存内容并删除。',
    quiz: ['保存检查点最重要的额外字段之一是？', ['随机表情', '数据格式版本', '模型公司 Logo'], 1, '版本使恢复逻辑能够判断兼容性。'], sources: ['storage', 'context'],
  },
  {
    id: 'skills', title: '按需要装载一份工作说明', subtitle: 'Skill、提示模板与渐进披露', minutes: 13, lab: 'context', goal: '解释技能描述与完整技能内容何时进入上下文。',
    sections: [
      ['技能包保存程序性知识', '一个技能包可以包含任务说明、参考资料和脚本，告诉 Agent 怎样完成某类工作。入口描述用于识别适用场景，完整说明只在需要时装入。技能并不等于新模型，也不是把文件放到任意目录就会自动生效；Harness 需要发现、匹配、加载和执行机制。'],
      ['用渐进披露控制成本', '开始只提供技能名称与简短描述，选择后再读取相关说明，必要时继续读取子资源。对一个文献核验任务，没有必要同时塞入所有网页设计和视频处理说明。装载策略要兼顾漏选与上下文膨胀，并保留实际载入了哪些文件的记录。'],
      ['说明不能越过权限边界', '技能文件可以过时、互相矛盾或包含不安全要求。固定工具权限不应因为技能文字而扩大，脚本也需要明确的执行许可与隔离。本站不下载或执行外部技能，只用上下文装箱实验说明“入口摘要”和“完整说明”的容量差别。'],
    ],
    code: [['const catalog = skills.map(s => ({ name: s.name, summary: s.summary }));', '伪代码：先暴露轻量目录。'], ['const selected = matchTask(task, catalog);', '匹配可以由规则或模型完成。'], ['const instructions = await loadSkill(selected.id);', '明确选中后再加载。'], ['context.add(instructions);', '技能内容仍受上下文预算约束。'], ['// 工具权限由运行时配置决定，不由技能文字授权。', '保留安全边界。']],
    source: 'web/lib/algorithms.js', codeLabel: '渐进装载伪代码；本站仅提供容量实验',
    pitfall: '把几十个完整技能同时放进系统提示词，会使上下文成本与指令冲突更难管理。', challenge: '比较装入“技能摘要”和“完整技能说明”时的空间，说明为什么先选再加载更容易扩展。',
    quiz: ['技能要求执行某脚本，就自动获得权限了吗？', ['是', '否，仍由运行时权限与隔离策略控制', '只要文件名正确就可以'], 1, '文件内容提供说明，授权由独立机制决定。'], sources: ['skills'],
  },
];

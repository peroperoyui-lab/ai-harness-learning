export default [
  {
    id: 'workflow', title: '让工作沿着清晰的路径流动', subtitle: '串行、路由、并行与依赖图', minutes: 15, lab: 'workflow', goal: '判断哪些步骤可以并行，哪些必须等待依赖。',
    sections: [
      ['固定流程也能很有用', '任务顺序已知时，可以用工作流明确规定步骤。例如先抽取字段，再验证格式，最后写入结果。路由根据输入类型选择分支；并行让独立子任务同时工作；汇总节点等待必要结果。每条边都应该表达依赖，而不只是图上好看的连线。'],
      ['时间与总工作量分开计算', '实验中两个互不依赖的检索任务耗时分别为 3 和 5 个教学时间单位，汇总耗时为 2。串行总时长是 10，并行理想时长是 7，但总工作量仍是 10。真实系统还存在并发限制、排队、调度开销和共享资源竞争。'],
      ['先定义失败传播策略', '一个并行分支失败，是全部失败、保留部分结果，还是允许补跑？这需要产品目标决定。Promise.all 会在任一 Promise 拒绝时拒绝，但不会自动取消其他已启动操作。需要独立的取消和结果收集策略，避免页面结束后仍有任务运行。'],
    ],
    code: [['const [a, b] = await Promise.all([searchA(), searchB()]);', '独立任务可以并发启动；函数在此处为示意。'], ['const summary = await summarize(a, b);', '汇总依赖两个结果，因此必须等待。'], ['// A → 汇总；B → 汇总', '依赖图决定可并发性。'], ['// 并行时长 ≈ max(A, B) + 汇总', '这是无排队、无额外开销的理想模型。']],
    source: 'web/lib/algorithms.js', codeLabel: '工作流伪代码；真实本地调度计算见 scheduleWorkflow',
    pitfall: '把互相依赖的任务强行并行，会让后续步骤拿到不完整输入。', challenge: '切换串行与并行，再修改第二个任务时长。解释为什么总工作量不变但关键路径改变了。',
    quiz: ['两个任务并行后，总 Token 成本一定下降吗？', ['一定', '不一定，工作量和重叠上下文可能不变或增加', '全部变成零'], 1, '并行主要改变等待时间，不自动降低模型工作量。'], sources: ['agents', 'async'],
  },
  {
    id: 'multi-agent', title: '多位专家怎样真正协作', subtitle: '角色、委派、交接、隔离与汇总', minutes: 16, lab: 'workflow', goal: '为多 Agent 任务明确输入、输出与负责边界。',
    sections: [
      ['角色必须对应工作职责', '给三个模型贴不同的专家标签，不自动构成可靠协作。更有用的设计是给每个子任务明确目标、上下文、可用工具、输出格式和完成条件。例如资料检索者提供证据清单，审查者核对证据支持程度，汇总者组织结论。'],
      ['委派与交接不同', '委派时主任务保留控制权，等待子任务结果；交接会把后续控制权转给另一个 Agent。共享工作区方便协作，也可能传播错误和敏感信息；独立上下文减少干扰，却需要额外的结果同步。应明确哪些信息共享，哪些只留在子任务里。'],
      ['收益需要与协调成本比较', '多个角色可能重复读取相同材料，产生冲突或相互附和。先比较单 Agent、固定工作流和多 Agent 的可验证结果，再决定复杂度。本站只模拟独立分支与汇总，不宣称运行了真实多模型圆桌，也不会因此产生多次 API 调用。'],
    ],
    code: [['const job = { goal: "核验引文", evidenceIds: ["doc-1"] };', '把任务写成明确合同。'], ['const review = await delegate("reviewer", job);', '伪代码：委派给审查角色。'], ['assertReviewSchema(review);', '输出也需验证。'], ['if (review.needsEvidence) return requestMoreEvidence();', '结果可以是不足，而不是强行通过。'], ['return synthesize(review);', '主任务保留汇总责任。']],
    source: 'web/lib/algorithms.js', codeLabel: '多 Agent 委派伪代码；本站实验模拟调度关系',
    pitfall: '多模型一致意见可能来自相同错误材料；一致性不能直接当作独立证据。', challenge: '把两个并行分支分别当作独立资料检索者，写出汇总节点必须等待的输出字段。',
    quiz: ['多 Agent 设计首先要明确什么？', ['每个角色的头像颜色', '任务边界、输入输出和控制权', '角色越多越好'], 1, '协作的基础是可验证的合同与责任。'], sources: ['agents'],
  },
  {
    id: 'mcp', title: '用统一接口连接外部能力', subtitle: 'MCP Host、Client、Server 与能力发现', minutes: 17, lab: 'protocol', goal: '区分模型供应商接口与 MCP 工具连接协议。',
    sections: [
      ['MCP 解决另一条连接', '模型 API 连接应用与模型服务；MCP 连接 AI 应用与外部工具或数据能力。Host 是用户使用的 AI 应用，Client 管理与某个 Server 的协议连接，Server 暴露能力。模型本身不必直接连接所有服务器；Host 负责把适用能力转给模型，并接收执行结果。'],
      ['能力不仅有工具', 'Tools 描述可执行能力，Resources 提供可读取内容，Prompts 提供可复用的交互模板。使用前需要初始化、能力协商和发现。传输可以基于本地进程或远程 HTTP，具体行为以版本化规范为准，不能把任意 JSON HTTP 接口都叫作完整 MCP。'],
      ['标准连接不等于可信连接', '工具描述、服务返回内容和授权上下文都需要检查。发现一个工具不表示用户已允许执行；远程连接还要考虑认证、授权范围和数据流向。本站提供概念和报文阅读训练，没有实现 MCP Client，也不会启动外部 MCP Server。'],
    ],
    code: [['const request = { jsonrpc: "2.0", id: 1, method: "tools/list" };', '已完成初始化之后的工具发现报文示意。'], ['const call = { jsonrpc: "2.0", id: 2, method: "tools/call",', '执行调用使用另一种方法。'], ['  params: { name: "calculator", arguments: { a: 23, b: 7 } } };', '参数须符合该 Server 的实际工具合同。'], ['// Host 仍负责用户授权和结果处理。', '协议互通与安全决策分离。']],
    source: 'web/lib/protocol.js', codeLabel: 'MCP 报文示意；本站协议实验实际比较的是模型 API',
    pitfall: 'MCP 工具接口与 OpenAI / Anthropic 模型接口不是同一层，不能混用路径与消息格式。', challenge: '在协议查看器定位模型请求中的 tools 字段，再说明它与 MCP 的 tools/list 发现步骤有什么不同。',
    quiz: ['MCP Server 返回一个新工具后，应该直接执行吗？', ['应该', '先校验来源、参数、权限和用户意图', '只看工具名称是否专业'], 1, '发现能力只是可用性信息，不是执行授权。'], sources: ['mcp'],
  },
  {
    id: 'artifacts', title: '从聊天输出变成可交付成果', subtitle: '结构化输出、验证、版本与来源', minutes: 15, lab: 'schema', goal: '区分“看起来像 JSON”与可供下游可靠使用的数据。',
    sections: [
      ['为下游约定结构', '当程序需要消费模型输出时，字段名、类型、必需项和允许值比文笔更重要。可以通过提示要求 JSON，也可以使用供应商支持的结构化输出能力。仅用提示要求 JSON 的方式仍可能得到格式错误，必须解析和验证。本站真实 JSON 演示使用提示约束，并明确显示验证结果。'],
      ['先验证，再保存为成果', '一个成果应包含内容、类型、来源、生成配置或版本信息。对于文件，还可以保存内容哈希、长度和创建时间，用于识别误改与追踪。模型声称“文件已保存”不等于文件真实存在，应用必须得到文件系统或成果存储的返回结果。'],
      ['把错误留在正确的层', 'JSON 解析失败是语法问题，字段类型不符是合同问题，内容与证据矛盾是事实问题。三种验证不能互相替代。本站只导出实际生成的学习记录 JSON；未实现的任意文档生成、上传与远端版本管理会在路线图中标记为扩展项。'],
    ],
    code: [['const result = JSON.parse(modelText);', '先验证能否解析。'], ['if (typeof result.concept !== "string") throw new Error("缺少 concept");', '再验证合同。'], ['if (typeof result.meaning !== "string") throw new Error("缺少 meaning");', '逐项检查，不只看第一项。'], ['const artifact = { version: 1, result, sourceIds };', '为实际交付内容附加来源与格式版本。']],
    source: 'web/lib/protocol.js', codeLabel: '结构化输出验证教学片段',
    pitfall: 'Schema 合格只说明结构正确；事实、引文和业务含义仍需另行核验。', challenge: '在参数实验尝试一个可解析但字段不匹配的对象；随后在 API 体验的 JSON 模式查看两层验证。',
    quiz: ['模型回复“已创建文件”，哪种证据更可靠？', ['这句话本身', '存储工具返回的实际文件标识与内容核验', '输出语气很确定'], 1, '成果应由真实执行与可检查内容来证明。'], sources: ['schema'],
  },
];
